<local:CountdownEditorViewBase
    x:Class="EventCountdowns.Views.CountdownEditorView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:EventCountdowns.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:xaml="using:EventCountdowns.Extensions.Xaml"
    xmlns:models="using:EventCountdowns.Core.Models"
    mc:Ignorable="d">
    <local:CountdownEditorViewBase.Transitions>
        <TransitionCollection>
            <NavigationThemeTransition>
                <NavigationThemeTransition.DefaultNavigationTransitionInfo>
                    <SlideNavigationTransitionInfo />
                </NavigationThemeTransition.DefaultNavigationTransitionInfo>
            </NavigationThemeTransition>
        </TransitionCollection>
    </local:CountdownEditorViewBase.Transitions>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup>
                <VisualState>
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="768" />
                    </VisualState.StateTriggers>
                    <VisualState.Setters>
                        <Setter Target="WrapperGridMaxColumn.MaxWidth" Value="1000" />
                        <Setter Target="FormSecondaryColumn.Width" Value="*" />
                        <Setter Target="BackgroundControlGrid.(Grid.Column)" Value="1" />
                        <Setter Target="BackgroundControlGrid.(Grid.Row)" Value="0" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <Border x:Name="HeaderGrid" Padding="20" Background="{StaticResource CommandBarBackgroundBrsuh}">
            <Grid>
                <Grid Visibility="{x:Bind Model.Mode, Mode=OneWay, ConverterParameter=Add, Converter={StaticResource EnumValueToVisibilityConverter}}">
                    <TextBlock Text="{xaml:Localize Key=AddEvent}" Style="{ThemeResource TitleTextBlockStyle}" Foreground="Black" />
                </Grid>
                <Grid Visibility="{x:Bind Model.Mode, Mode=OneWay, ConverterParameter=Edit, Converter={StaticResource EnumValueToVisibilityConverter}}">
                    <TextBlock Text="{xaml:Localize Key=EditEvent}" Style="{ThemeResource TitleTextBlockStyle}" Foreground="Black" />
                </Grid>
            </Grid>

        </Border>
        <ScrollViewer Grid.Row="1" Margin="0">
            <Border Padding="10" HorizontalAlignment="Stretch">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" MaxWidth="350" x:Name="WrapperGridMaxColumn"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="0" x:Name="FormSecondaryColumn" />
                        </Grid.ColumnDefinitions>
                        <StackPanel x:Name="BasicInputFormStack" Margin="10" HorizontalAlignment="Stretch">
                            <TextBlock Text="{xaml:Localize Key=Name}" Style="{StaticResource FormLabelTextBlockStyle}" Margin="0,0,0,8" />
                            <TextBox Text="{x:Bind Model.Name, Mode=TwoWay}" />
                            <TextBlock Text="{xaml:Localize Key=Date}" Style="{StaticResource FormLabelTextBlockStyle}" />
                            <DatePicker Date="{x:Bind Model.Date, Mode=TwoWay}" />
                            <TextBlock Text="{xaml:Localize Key=Time}" Style="{StaticResource FormLabelTextBlockStyle}" />
                            <TimePicker Time="{x:Bind Model.Time, Mode=TwoWay}" />
                            <TextBlock Text="{xaml:Localize Key=Celebration}" Style="{StaticResource FormLabelTextBlockStyle}" />
                            <TextBox Text="{x:Bind Model.CelebrationMessage, Mode=TwoWay}" PlaceholderText="{x:Bind Model.DefaultCelebrationMessage, Mode=OneWay}" />

                        </StackPanel>
                        <Grid Grid.Row="1" Margin="10" x:Name="BackgroundControlGrid">
                            <Grid.RowDefinitions>
                                <RowDefinition Height="Auto" />
                                <RowDefinition Height="Auto" />
                            </Grid.RowDefinitions>
                            <TextBlock Text="{xaml:Localize Key=Background}"  Margin="0,0,0,8" Style="{StaticResource FormLabelTextBlockStyle}" />
                            <Grid Grid.Row="1">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <StackPanel>
                                    <Button Command="{x:Bind Model.ChooseYourImageCommand}" Content="{xaml:Localize Key=ChooseYourImage}" Margin="0,0,0,10" />
                                    <TextBlock Text="{xaml:Localize Key=DefaultBackgrounds}" FontWeight="Normal" FontSize="16" />
                                    <GridView SelectionMode="Single" Margin="0,5,0,0" ItemsSource="{x:Bind Model.DefaultBackgrounds}"  SelectedItem="{Binding SelectedDefaultBackground, Mode=TwoWay}">
                                        <GridView.ItemTemplate>
                                            <DataTemplate x:DataType="models:DefaultBackground">
                                                <Border Background="{ThemeResource SystemControlBackgroundAccentBrush}">
                                                    <Image Stretch="UniformToFill" Width="70" Height="70" Source="{x:Bind ThumbnailPath}" />
                                                </Border>
                                            </DataTemplate>
                                        </GridView.ItemTemplate>
                                    </GridView>

                                </StackPanel>
                                <Grid Grid.Column="1" VerticalAlignment="Top" Margin="20,0,0,0" Height="180">
                                    <Border HorizontalAlignment="Stretch" Background="{ThemeResource SystemControlBackgroundAccentBrush}" />
                                    <Image Stretch="UniformToFill" HorizontalAlignment="Center" VerticalAlignment="Center">
                                        <Image.Source>
                                            <Binding Path="BackgroundPath">
                                                <Binding.TargetNullValue>
                                                    <ImageSource>/Assets/SampleBackgrounds/Thumbnails/BlankBackground.png</ImageSource>
                                                </Binding.TargetNullValue>
                                            </Binding>
                                        </Image.Source>
                                    </Image>
                                    <TextBlock Text="{xaml:Localize Key=Preview}" Margin="10" VerticalAlignment="Bottom" Foreground="#FFF" Opacity="0.8" />
                                </Grid>
                            </Grid>
                        </Grid>
                    </Grid>
                </Grid>

            </Border>
        </ScrollViewer>
        <CommandBar Grid.Row="2">
            <AppBarButton Icon="Save" Label="{xaml:Localize Key=Save}" Command="{x:Bind Model.SaveCommand}" />
            <AppBarButton Icon="Cancel" Label="{xaml:Localize Key=Cancel}" Command="{x:Bind Model.CancelCommand}" />
        </CommandBar>
        <Grid Grid.RowSpan="3" Background="#88FFFFFF" Visibility="{x:Bind Model.IsWorking, Mode=OneWay, Converter={StaticResource BoolToVisibilityConverter}, FallbackValue=Collapsed}">
            <ProgressRing Foreground="Black" BorderBrush="Black" IsActive="{x:Bind Model.IsWorking, Mode=OneWay}" Width="50" Height="50" />
        </Grid>
    </Grid>
</local:CountdownEditorViewBase>
